[phases.setup]
nixPkgs = ["python313"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate && python manage.py collectstatic --noinput && python -m gunicorn portfolio_project.wsgi --log-file -"